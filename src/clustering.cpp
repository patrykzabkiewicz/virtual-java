
#include <stdlib.h>

/*
	square clustering
	cluster graph by dividing verticies into square segments
*/
int * square_cluster(int * graph);

/*
	nearest neighbor clustering
	cluster graph by squeze nearest neighbors into one node
*/
int * nn_cluster(int * graph);


int * square_cluster(int * graph) {

}

int * nn_cluster(int * graph) {

}
